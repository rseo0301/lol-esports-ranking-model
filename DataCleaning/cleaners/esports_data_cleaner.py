# Cleaner for all data files, except for "games"
# Cleans mapping_data.json
# Takes in a JSON object, cleans it, and outputs a JSON object that we can store in our database

from typing import List

def cleanLeaguesData(data: List[dict]) -> List[dict]:
    return data

def cleanMappingData(data: List[dict]) -> List[dict]:
    return data

def cleanPlayersData(data: List[dict]) -> List[dict]:
    return data

def cleanTeamsData(data: List[dict]) -> List[dict]:
    return data

def cleanTournamentsData(data: List[dict]) -> List[dict]:
    return data
